# Clear the zeroserve resource registry

This stops serving all currently registered resources and frees
associated memory buffers.

## Usage

``` r
zs_clear_registry()
```

## Value

Logical; TRUE if registry was cleared.

## Examples

``` r
if (FALSE) { # \dontrun{
zs_clear_registry()
} # }
```
